"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { AppShell } from "@/src/components/app-shell";

type Task = { title: string; detail: string; tag: string; color: string; done: boolean };

const initialTasks: Task[] = [
  { title: "Review product spec document", detail: "Today at 9:30 AM", tag: "Work", color: "#1664d8", done: true },
  { title: "Finish app architecture draft", detail: "Today at 2:00 PM", tag: "Work", color: "#1664d8", done: false },
  { title: "Prepare product sync notes", detail: "Today at 4:30 PM", tag: "Work", color: "#f2a33a", done: false },
  { title: "Pick up groceries", detail: "Tomorrow at 10:00 AM", tag: "Personal", color: "#7b8da5", done: false },
];

export function TasksWorkspace() {
  const [tasks, setTasks] = useState(initialTasks);
  const [filter, setFilter] = useState("All");
  const visibleTasks = filter === "Completed" ? tasks.filter((task) => task.done) : filter === "Today" ? tasks.filter((task) => task.detail.startsWith("Today")) : tasks;
  const completedCount = tasks.filter((task) => task.done).length;

  function toggleTask(index: number) {
    setTasks((current) => current.map((task, taskIndex) => taskIndex === index ? { ...task, done: !task.done } : task));
  }

  return <AppShell active="tasks"><header className="flex flex-col gap-4 border-b border-[#dbe4ef] pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-[#1664d8]">Tuesday, September 8</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Tasks</h1><p className="mt-2 text-sm text-[#60738c]">Keep the important work visible and moving.</p></div><Link href="/task-creation" className="inline-flex w-fit items-center rounded-lg bg-[#155fcf] px-4 py-3 text-sm font-semibold text-white transition hover:bg-[#0d4fac]">Add task <span className="ml-3" aria-hidden="true">＋</span></Link></header><div className="mt-7 grid gap-5 lg:grid-cols-[1fr_280px]"><section className="rounded-2xl border border-[#dbe4ef] bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#7b8da5]">Inbox</p><h2 className="mt-1 text-lg font-semibold">Your task list</h2></div><span className="text-sm text-[#7b8da5]">{completedCount} of {tasks.length} complete</span></div><div className="mt-5 flex gap-2 overflow-x-auto border-b border-[#edf1f6] pb-4">{["All", "Today", "Completed"].map((option) => <button key={option} type="button" onClick={() => setFilter(option)} className={`shrink-0 rounded-lg px-3 py-2 text-sm font-medium ${filter === option ? "bg-[#eaf2ff] text-[#155fcf]" : "text-[#7b8da5] hover:bg-[#f5f8fc]"}`}>{option}</button>)}</div><div className="divide-y divide-[#edf1f6]">{visibleTasks.map((task) => { const index = tasks.indexOf(task); return <div key={task.title} className="flex items-start gap-3 py-4"><button type="button" onClick={() => toggleTask(index)} aria-label={`${task.done ? "Mark incomplete" : "Complete"} ${task.title}`} className={`mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full border-2 text-xs ${task.done ? "border-[#1664d8] bg-[#1664d8] text-white" : "border-[#b4c3d4] hover:border-[#1664d8]"}`}>{task.done ? "✓" : ""}</button><div className="min-w-0 flex-1"><p className={`text-sm font-medium ${task.done ? "text-[#9aa9ba] line-through" : "text-[#243b5a]"}`}>{task.title}</p><p className="mt-1 text-xs text-[#7b8da5]">{task.detail} <span className="ml-2 rounded bg-[#f1f5fa] px-2 py-1" style={{ color: task.color }}>{task.tag}</span></p></div><button type="button" className="px-2 text-lg leading-none text-[#9aa9ba]" aria-label={`More options for ${task.title}`}>···</button></div>; })}</div></section><aside className="space-y-5"><section className="rounded-2xl border border-[#dbe4ef] bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#7b8da5]">Today at a glance</p><div className="mt-5 grid grid-cols-2 gap-4"><div><p className="text-2xl font-semibold">{tasks.filter((task) => !task.done).length}</p><p className="mt-1 text-xs text-[#7b8da5]">Open tasks</p></div><div><p className="text-2xl font-semibold">2</p><p className="mt-1 text-xs text-[#7b8da5]">Due today</p></div></div></section><section className="rounded-2xl border border-[#cce0fa] bg-[#f1f7ff] p-5"><p className="text-sm font-semibold text-[#155fcf]">Need a clearer plan?</p><p className="mt-2 text-sm leading-6 text-[#60738c]">Ask Zenvyx to break down a task or order your next steps.</p><Link href="/ai-chat" className="mt-4 inline-flex text-sm font-semibold text-[#155fcf]">Open AI workspace <span className="ml-2">→</span></Link></section></aside></div></AppShell>;
}

export function FocusWorkspace() {
  const [seconds, setSeconds] = useState(25 * 60);
  const [running, setRunning] = useState(false);
  useEffect(() => { if (!running || seconds <= 0) return; const timer = window.setInterval(() => setSeconds((value) => value - 1), 1000); return () => window.clearInterval(timer); }, [running, seconds]);
  const minutes = Math.floor(seconds / 60).toString().padStart(2, "0");
  const remainder = (seconds % 60).toString().padStart(2, "0");
  return <AppShell active="focus"><header className="flex flex-col gap-2 border-b border-[#dbe4ef] pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-[#1664d8]">Focus mode</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Make room for deep work.</h1><p className="mt-2 text-sm text-[#60738c]">One protected block is enough to change the shape of your day.</p></div><Link href="/focus-stats" className="text-sm font-semibold text-[#1664d8]">View focus history →</Link></header><div className="mt-7 grid gap-5 lg:grid-cols-[1fr_320px]"><section className="rounded-2xl bg-[#102f62] p-7 text-white shadow-[0_16px_35px_rgba(16,47,98,0.18)] sm:p-10"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-blue-100">Deep work</p><p className="mt-1 text-xs text-blue-200">Architecture draft · 25 minutes</p></div><span className="rounded-full bg-white/10 px-3 py-1.5 text-xs text-blue-100">{running ? "In progress" : "Ready"}</span></div><div className="my-12 flex justify-center"><div className="flex h-52 w-52 items-center justify-center rounded-full border-[10px] border-blue-300/20 border-t-blue-300 text-5xl font-semibold tracking-tight">{minutes}:{remainder}</div></div><div className="flex justify-center gap-3"><button type="button" onClick={() => setRunning((value) => !value)} className="rounded-lg bg-white px-5 py-3 text-sm font-semibold text-[#1557b5]">{running ? "Pause session" : "Start session"}</button><button type="button" onClick={() => { setRunning(false); setSeconds(25 * 60); }} className="rounded-lg bg-white/10 px-5 py-3 text-sm font-semibold text-white hover:bg-white/15">Reset</button></div></section><aside className="space-y-5"><section className="rounded-2xl border border-[#dbe4ef] bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#7b8da5]">This week</p><div className="mt-5 grid grid-cols-2 gap-4"><div><p className="text-2xl font-semibold">4h 20m</p><p className="mt-1 text-xs text-[#7b8da5]">Focus time</p></div><div><p className="text-2xl font-semibold">12</p><p className="mt-1 text-xs text-[#7b8da5]">Sessions</p></div></div></section><section className="rounded-2xl border border-[#dbe4ef] bg-white p-5 shadow-sm"><p className="text-sm font-semibold">Choose a session</p><div className="mt-4 space-y-2">{[["Deep work", "25 min"], ["Reading", "20 min"], ["Reset", "15 min"]].map(([name, time]) => <button key={name} type="button" onClick={() => { setSeconds(Number.parseInt(time) * 60); setRunning(false); }} className="flex w-full items-center justify-between rounded-lg border border-[#edf1f6] px-3 py-3 text-left text-sm text-[#48627e] hover:border-[#b9d2f4] hover:bg-[#f7faff]"><span>{name}</span><span className="text-xs text-[#7b8da5]">{time}</span></button>)}</div></section></aside></div></AppShell>;
}

export function ProfileWorkspace() {
  const [name, setName] = useState("Alex Rivera");
  const [saved, setSaved] = useState(false);
  function saveProfile(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaved(true); }
  const links = [["Goals & milestones", "Track annual objectives", "/goals-milestones"], ["Habits", "Manage daily routines", "/habits-tracker"], ["Notifications", "Preferences and alerts", "/notifications-settings"], ["App settings", "Configure your workspace", "/settings"]];
  return <AppShell active="profile"><header className="border-b border-[#dbe4ef] pb-6"><p className="text-sm font-medium text-[#1664d8]">Account</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Profile & preferences</h1><p className="mt-2 text-sm text-[#60738c]">Keep your personal details and workspace preferences up to date.</p></header><div className="mt-7 grid gap-5 lg:grid-cols-[1fr_320px]"><section className="rounded-2xl border border-[#dbe4ef] bg-white p-6 shadow-sm"><div className="flex items-center gap-4 border-b border-[#edf1f6] pb-6"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#dceaff] text-xl font-semibold text-[#1557b5]">A</div><div><h2 className="text-lg font-semibold">{name}</h2><p className="mt-1 text-sm text-[#7b8da5]">alex@nexus.ai</p><p className="mt-1 text-xs text-[#1664d8]">Member since January 2025</p></div></div><form onSubmit={saveProfile} className="mt-6 space-y-5"><label className="block text-sm font-medium text-[#243b5a]">Full name<input value={name} onChange={(event) => { setName(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-lg border border-[#cbd8e7] px-3 py-3 text-sm outline-none focus:border-[#1664d8] focus:ring-2 focus:ring-[#1664d8]/10" /></label><label className="block text-sm font-medium text-[#243b5a]">Email address<input defaultValue="alex@nexus.ai" type="email" className="mt-2 w-full rounded-lg border border-[#cbd8e7] px-3 py-3 text-sm outline-none focus:border-[#1664d8] focus:ring-2 focus:ring-[#1664d8]/10" /></label><label className="block text-sm font-medium text-[#243b5a]">About you<textarea defaultValue="Building a calm, high-performance life with AI." rows={3} className="mt-2 w-full resize-none rounded-lg border border-[#cbd8e7] px-3 py-3 text-sm outline-none focus:border-[#1664d8] focus:ring-2 focus:ring-[#1664d8]/10" /></label><div className="flex items-center gap-4"><button type="submit" className="rounded-lg bg-[#155fcf] px-4 py-3 text-sm font-semibold text-white hover:bg-[#0d4fac]">Save changes</button>{saved && <span className="text-sm text-[#2ea66f]">Profile updated</span>}</div></form></section><aside className="space-y-5"><section className="rounded-2xl border border-[#dbe4ef] bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#7b8da5]">Your activity</p><div className="mt-5 grid grid-cols-3 gap-3 text-center"><div><p className="text-xl font-semibold">1,247</p><p className="mt-1 text-xs text-[#7b8da5]">Completed</p></div><div><p className="text-xl font-semibold">342h</p><p className="mt-1 text-xs text-[#7b8da5]">Focused</p></div><div><p className="text-xl font-semibold">28d</p><p className="mt-1 text-xs text-[#7b8da5]">Streak</p></div></div></section><section className="rounded-2xl border border-[#dbe4ef] bg-white p-3 shadow-sm">{links.map(([title, detail, href]) => <Link key={title} href={href} className="flex items-center gap-3 rounded-lg px-3 py-3 hover:bg-[#f5f8fc]"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#eaf2ff] text-sm font-semibold text-[#1664d8]">→</span><span className="flex-1"><span className="block text-sm font-medium text-[#243b5a]">{title}</span><span className="mt-1 block text-xs text-[#7b8da5]">{detail}</span></span><span className="text-[#9aa9ba]">›</span></Link>)}</section></aside></div></AppShell>;
}
